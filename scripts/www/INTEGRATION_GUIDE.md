# ASEAN NEV Insight - 功能整合指南

## 功能联动说明

本项目实现了搜索、估价、预测三大核心功能的深度整合，提供无缝的用户体验。

## 功能流程图

```
┌─────────────────────────────────────────────────────────────────┐
│                        数据看板 (Dashboard)                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🔍 快速搜索栏                                           │   │
│  │  [输入车型名称] [搜索按钮]                               │   │
│  │  热门：BYD Atto 3 | Tesla Model 3 | MG ZS EV            │   │
│  └─────────────────────────────────────────────────────────┘   │
│                         ↓ 点击搜索                              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      车型搜索 (Search)                           │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  📊 Power BI 数据展示                                    │   │
│  │  • 销量趋势图 / 价格趋势图 / 价格分布图                    │   │
│  │  • 详细数据表格（月份/销量/价格/环比）                     │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  🤖 AI 智能建议                                          │   │
│  │  • 市场建议 / 价格趋势 / 交易建议                         │   │
│  │  [立即估价] [查看预测] ← 快捷跳转按钮                     │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
          ↓ 点击[立即估价]                    ↓ 点击[查看预测]
┌─────────────────┐                ┌─────────────────┐
│   AI估价页面     │                │   市场预测页面   │
│  (自动填充车型)  │                │  (自动填充车型)  │
└─────────────────┘                └─────────────────┘
```

## 功能联动详情

### 1. 快速搜索 → 车型搜索

**位置**：数据看板顶部的搜索栏

**功能**：
- 输入车型名称快速搜索
- 点击热门标签直接搜索
- 自动跳转到搜索页面并执行搜索

**代码实现**：
```javascript
// uiManager.js
performQuickSearch(keyword, params) {
  // 1. 解析关键词
  const parsed = this.parseSearchKeyword(keyword);
  
  // 2. 跳转到搜索页面
  this.switchTab('search');
  
  // 3. 自动填充表单
  this.elements.searchBrand.value = params.brand;
  this.elements.searchModel.value = params.model;
  
  // 4. 自动提交搜索
  this.elements.marketSearchForm.dispatchEvent(new Event('submit'));
}
```

### 2. 车型搜索 → AI估价

**位置**：搜索结果页面的 "AI 智能建议" 卡片

**功能**：
- 点击 "立即估价" 按钮
- 自动跳转到估价页面
- 自动填充已搜索的品牌、车型、国家
- 用户只需补充年份、里程、车况即可完成估价

**代码实现**：
```javascript
// uiManager.js
bindSearchActionButtons(searchParams) {
  valuationBtn.onclick = () => {
    // 1. 切换到估价标签
    this.app.switchTab('valuation');
    
    // 2. 自动填充表单
    this.autoFillValuationForm(searchParams);
  };
}

autoFillValuationForm(params) {
  this.elements.valBrand.value = params.brand;
  this.elements.valModel.value = params.model;
  // ... 自动触发联动选择
}
```

### 3. 车型搜索 → 市场预测

**位置**：搜索结果页面的 "AI 智能建议" 卡片

**功能**：
- 点击 "查看预测" 按钮
- 自动跳转到预测页面
- 自动填充已搜索的车型
- 直接显示未来价格走势预测

**代码实现**：
```javascript
// uiManager.js
predictionBtn.onclick = () => {
  this.app.switchTab('prediction');
  this.autoFillPredictionForm(searchParams);
};
```

### 4. AI建议生成

根据搜索结果自动生成个性化建议：

```javascript
// uiManager.js
generateSearchAiSuggestions(results) {
  // 根据销量趋势生成市场建议
  if (summary.volumeTrend > 10) {
    marketAdvice = '当前市场对该车型需求旺盛...';
  }
  
  // 根据价格趋势生成投资建议
  if (trends.price === 'up') {
    priceTrendText = '价格呈上升趋势，建议持有...';
  }
  
  // 生成交易建议（售价/成交周期）
}
```

## 数据流转

### 搜索参数结构
```javascript
{
  brand: 'byd',        // 品牌
  model: 'atto3',      // 车型
  period: '12m',       // 时间周期
  country: 'th'        // 国家
}
```

### 搜索结果数据
```javascript
{
  summary: {
    totalVolume: 1234,      // 总销量
    avgPrice: 1250000,      // 平均价格
    priceRange: '1.0-1.5M', // 价格区间
    priceTrend: 5.2         // 价格趋势(%)
  },
  monthlyData: [...],       // 月度详细数据
  priceDistribution: [...], // 价格分布
  trends: {
    price: 'up',            // 价格趋势方向
    volume: 'stable'        // 销量趋势方向
  }
}
```

## 用户场景示例

### 场景1：快速了解某车型市场情况

1. 在数据看板点击 "BYD Atto 3" 热门标签
2. 自动跳转到搜索页面并显示：
   - 近12个月销量趋势
   - 价格走势
   - AI生成的市场建议

### 场景2：卖家估价流程

1. 搜索 "Tesla Model 3" 查看市场
2. 看到AI建议："当前需求稳定，建议售价฿1.75M"
3. 点击 "立即估价" 按钮
4. 跳转到估价页面，品牌/车型已填充
5. 输入年份2022、里程3万公里、车况良好
6. 获取AI估价结果

### 场景3：投资决策流程

1. 搜索 "MG ZS EV" 查看市场
2. 看到价格趋势："过去12个月上涨8.5%"
3. 点击 "查看预测" 按钮
4. 查看未来6个月价格预测
5. 根据预测结果决定购买/出售时机

## 技术实现要点

### 1. 跨标签页数据传递

使用 `app.searchResults` 存储搜索结果，各页面共享：

```javascript
// main.js
async searchMarketData(params) {
  const results = await this.marketSearchEngine.search(params);
  this.searchResults = results;  // 存储结果供其他页面使用
}
```

### 2. 表单自动填充

使用事件触发机制确保联动选择正常：

```javascript
// 填充品牌后触发change事件加载车型列表
this.elements.searchBrand.value = brand;
this.elements.searchBrand.dispatchEvent(new Event('change'));

// 延迟填充车型（等待列表加载）
setTimeout(() => {
  this.elements.searchModel.value = model;
}, 100);
```

### 3. 延迟初始化

标签页切换后延迟初始化图表（确保DOM可见）：

```javascript
initSearchTab() {
  setTimeout(() => {
    if (this.searchResults) {
      this.renderSearchCharts();
    }
  }, 100);
}
```

## 扩展建议

### 1. 添加历史记录

保存用户的搜索历史，方便快速访问：

```javascript
// 保存搜索历史
localStorage.setItem('searchHistory', JSON.stringify(history));

// 在搜索框下方显示历史记录
```

### 2. 对比功能

允许同时对比多款车型：

```javascript
// 添加对比车型
const compareList = ['byd-atto3', 'tesla-model3'];

// 在图表中显示多条趋势线
```

### 3. 价格预警

设置价格变动提醒：

```javascript
// 用户设置目标价格
const targetPrice = 1100000;

// 当市场价格达到目标时发送通知
```

## 文件对应关系

| 功能 | 主要文件 | 关键方法 |
|------|---------|---------|
| 快速搜索 | uiManager.js | `bindQuickSearch()`, `performQuickSearch()` |
| 搜索执行 | main.js | `searchMarketData()` |
| 搜索展示 | uiManager.js | `showSearchResults()`, `renderSearchTable()` |
| 搜索图表 | chartManager.js | `renderSearchVolumeChart()`, `renderSearchPriceChart()` |
| AI建议 | uiManager.js | `generateSearchAiSuggestions()` |
| 跳转到估价 | uiManager.js | `bindSearchActionButtons()`, `autoFillValuationForm()` |
| 跳转到预测 | uiManager.js | `autoFillPredictionForm()` |

## 调试提示

1. **表单填充失败**：检查 select 元素的 option 是否已加载
2. **图表不显示**：确认容器元素在图表渲染时可见
3. **数据不更新**：检查 `app.searchResults` 是否正确存储
